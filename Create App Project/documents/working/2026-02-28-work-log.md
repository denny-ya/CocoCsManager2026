# 작업 일지 - 2026년 2월 28일

## 📋 작업 개요
BS 검색 기능 개선 및 AS 일일실적 메뉴 신규 개발

---

## 1. 바코드 검색 V열 연동

### 변경 내용
- 스프레드시트 V열에 **차대번호(바코드)** 열 추가
- 기존 B열(차대번호)과 V열(바코드) 동시 매칭 검색 지원
- 바코드 스캔 후 **자동 검색 + 매칭 차대번호 표시** 기능 추가

### 수정 파일
| 파일 | 내용 |
|------|------|
| `scripts/gas-api-code.js` | V열(index 21) 캐싱, 검색 매칭, formatVehicleData 추가 |
| `services/mockData.ts` | `barcodeVin` 타입 추가 |
| `services/api.ts` | 새 GAS 배포 URL 적용 |
| `.env` | 새 배포 URL 반영 |
| `app/bs-search/search.tsx` | 바코드 스캔 → 자동검색 → 매칭VIN 알림 표시 |

### GAS 검색 로직 (정확/부분/끝자리 매칭)
```javascript
// B열(차대번호) 또는 V열(바코드 차대번호) 매칭
if (vinUpper === searchStr || barcodeUpper === searchStr) {
    exactMatches.push(item);
} else if (searchStr.length >= 4 && (vinUpper.endsWith(searchStr) || barcodeUpper.endsWith(searchStr))) {
    endsWithMatches.push(item);
} else if (vinUpper.includes(searchStr) || barcodeUpper.includes(searchStr)) {
    partialMatches.push(item);
}
```

---

## 2. 마스터 통계 0% 개선

### 변경 내용
- 파트(K열)와 마스터(L열)가 매칭되지 않아 `total === 0`인 경우
- 0% 통계 대신 **"해당 파트에 배정된 데이터가 없습니다"** 메시지 표시

### 수정 파일
- `app/bs-search/search.tsx` — `masterStats.total === 0` 조건 분기 추가

---

## 3. AS 일일실적 메뉴 신규 개발

### 데이터 소스
- 스프레드시트: `1FvByt2ivNffTArrTaKYX9Cawze-30YL2paHlKUsukGY`
- 전체 51개 열 중 핵심 10개 열 활용

### 핵심 열 매핑
| 열 | 인덱스 | 항목 | 용도 |
|----|--------|------|------|
| A | 0 | 완료구분 | 완료/미완료 건수 |
| B | 1 | 접수상태 | 접수대기 구분 |
| C | 2 | 상태구분 | 전화상담(미출동) = 미출동 |
| H | 7 | 파트 | 서비스1~4 그룹 분류 |
| J | 9 | 담당마스터 | 개인별 실적 |
| M | 12 | 접수일자 | 당일/월별 필터 |
| Y | 24 | 완료일자 | 당일 완료 건수 |
| AH | 34 | 접수취소일자 | 취소 건수 |
| AK | 37 | 리드타임(고객) | 평균 리드타임 |
| AP | 42 | 지점 | 센터 분류 |

### 집계 로직
| 항목 | 계산 방식 |
|------|-----------|
| AS완료-미출동 | A열=완료 & C열=전화상담(미출동) |
| AS완료-출동 | A열=완료 & C열≠전화상담(미출동) |
| 미완료(4일+) | A열=미완료 & 접수일자 ≤ 오늘-4일 |

### 생성 파일
| 파일 | 설명 |
|------|------|
| `scripts/gas-as-api-code.js` | 🆕 AS 실적 GAS API (별도 배포) |
| `app/statistics/as-performance.tsx` | 🆕 AS 실적 화면 |
| `services/api.ts` | `getAsPerformance()` 함수 추가 |
| `app/statistics.tsx` | 라우트 연결 활성화 |
| `app/_layout.tsx` | 서브 라우트 등록 |
| `.env` | `EXPO_PUBLIC_AS_API_URL` 추가 |

### 화면 구성
```
검색 조건 화면 (첫 화면)
├── 완료일자: 달력 팝업 (일 단위)
├── 수리구분: 경수리 / 중수리
├── 파트: 서비스1~4 (중수리 시 팩토리 추가)
└── [검색] 버튼

검색 결과 화면
├── 요약 카드 4개 (접수/완료/미완료/리드타임)
└── 서비스→센터→마스터 아코디언 테이블
```

### GAS API 배포 완료
- URL: `AKfycbztUQFpRkR60zXio5WmJPKPseOWU8czXD2xoIXhaM5D5B2LifaT68o3rX7YNZ2WooRo`

---

## 4. Git 푸시

- **태그**: `v0.2.1`
- **메시지**: `[검색] BS 및 리워크 메뉴의 안정성 테스트 완료 - 검색 로딩시간이 소요되는 문제 해결 필요`
- **리포지토리**: `https://github.com/denny-ya/CocoCsManager2026`

---

## 🔜 다음 세션 진행 예정
1. AS 실적 화면 실데이터 검증 및 수정
2. 경수리/중수리 분류 로직 GAS API 반영
3. 검색 로딩 시간 최적화
