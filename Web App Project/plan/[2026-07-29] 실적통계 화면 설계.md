# [2026-07-29] 실적통계 화면 설계

## 1. 문서 목적
- `AS일일실적`을 기존 엑셀 공유 방식에서 Web App 화면으로 전환하기 위한 설계 문서입니다.
- 현재 분석한 raw 데이터 구조, 통합 방향, UI 설계 방향, 가공 테이블 구조, 향후 결정사항을 계속 누적합니다.
- 이 문서는 구현 전까지 살아있는 설계 노트로 사용하며, 추가 분석 결과가 생기면 같은 문서에 이어서 업데이트합니다.

## 2. 현재 목표
- Apps Script Web App 안에서 `AS 일일실적` 화면을 제공한다.
- 기준일, 파트, 수리구분을 선택해 일일 실적을 조회한다.
- 모바일에서도 보기 좋은 현대적인 대시보드 UI를 적용한다.
- raw 데이터를 화면에서 직접 복잡하게 계산하지 않고, 앱이 읽기 쉬운 집계 데이터로 가공한 뒤 표시한다.

## 3. 검토한 자료
### 3.1 기존 실적 관리 엑셀
- 파일명: `AS실적.xlsx`
- 위치: `C:\Users\전은진\Downloads\AS실적.xlsx`
- 확인된 시트:
  - `일일실적`
  - `접수RAW`
  - `RAW`

### 3.2 시스템 무가공 raw 데이터
- 파일명: `AS접수목록 - 2026-07-29T165423.119.xlsx`
- 위치: `C:\Users\전은진\Downloads\AS접수목록 - 2026-07-29T165423.119.xlsx`
- 확인된 시트:
  - `Sheet0`
- 실제 데이터 구조:
  - 3,662행
  - 51열
  - 원본 열 범위: `A~AY`

## 4. 기존 엑셀 구조 분석
### 4.1 `AS실적.xlsx`의 raw 구조
- `접수RAW`와 `RAW`는 모두 70개 컬럼으로 구성되어 있습니다.
- 두 시트의 헤더 구조는 동일합니다.
- `RAW`의 접수번호는 모두 `접수RAW` 안에 포함되어 있습니다.
- `접수번호` 중복은 확인되지 않았습니다.
- 두 raw의 주요 필드는 대부분 동일하며, 차이는 `센터완료` 보조 컬럼 일부에서만 확인되었습니다.

### 4.2 기존 수식 구조
- `일일실적` 시트는 `접수RAW`와 `RAW`를 `COUNTIFS`, `AVERAGEIFS` 등으로 집계합니다.
- `접수RAW`는 주로 접수/미완료 기준 집계에 사용됩니다.
- `RAW`는 주로 완료/취소/리드타임 기준 집계에 사용됩니다.
- 현재 엑셀에는 보조 컬럼이 포함되어 있습니다.
  - 접수일시
  - 접수확정일시
  - 접수월
  - 접수일
  - 접수누적
  - 약속일시
  - 완료일시
  - 완료일
  - 완료누적
  - 이관
  - 공장완료월
  - 공장완료
  - 리드타임
  - 리드타임(공장)
  - 리드타임(센터)
  - 장기미완료
  - 센터완료월
  - 센터완료

### 4.3 기존 수식의 주의점
- 일부 보조 수식에서 행 참조가 어긋난 것으로 보이는 부분이 있습니다.
- 예: `RAW` 시트의 `센터완료` 보조 컬럼에서 2행 기준 수식이 `C2`가 아닌 다른 행을 참조하는 사례가 확인되었습니다.
- 따라서 앱에서는 기존 보조 수식을 그대로 신뢰하기보다, 원본 컬럼 기준으로 가공 값을 새로 계산하는 방향이 안전합니다.

## 5. raw 통합 방향
### 5.1 결론
- raw는 하나의 통합 원본 테이블로 관리하는 것이 가능합니다.
- 통합 기준 키는 `접수번호`입니다.
- 최종적으로 `AS_RAW_UNIFIED` 테이블을 만들고, 원본 51개 컬럼을 그대로 보관하는 구조를 권장합니다.

### 5.2 권장 원칙
- `AS_RAW_UNIFIED`는 무가공 원본 보관용입니다.
- 수식/판단값/집계값을 원본 테이블에 섞지 않습니다.
- 앱 화면용 판단값은 별도 가공 테이블인 `AS_DAILY_WORK`에서 생성합니다.
- UI는 최종 집계 테이블인 `AS_DAILY_SUMMARY`만 읽도록 설계합니다.

## 6. `AS_RAW_UNIFIED` 원본 컬럼
시스템에서 내려받는 무가공 raw 기준으로 `A~AY` 51개 컬럼을 원본 그대로 보관합니다.

| 원본열 | 컬럼명 | 주요 용도 |
| --- | --- | --- |
| A | 완료구분 | 완료/미완료 기본 상태 |
| B | 접수상태 | 접수대기, 방문약속, AS완료, 접수취소 등 |
| C | 상태구분 | 출동/미출동/중수리 판단 핵심 |
| D | 접수번호 | 고유키, raw 통합 기준 |
| E | 접수구분 | 일반A/S, 이동 등 |
| F | 접수경로 | 영업점접수/일반접수 |
| G | 접수분류 | 고장 유형 |
| H | 파트 | 서비스1~4 구분 |
| I | 담당마스터코드 | 담당자 고유코드 |
| J | 담당마스터 | 상세 행/센터 매핑 기준 |
| K | 카트버전 | 참고/필터 확장 가능 |
| L | 차대번호 | 건별 추적용 |
| M | 접수일자 | AS접수 당일/누적 계산 |
| N | 접수시간 | 접수일시 계산 |
| O | 접수확정일자 | 접수확정 상태 참고 |
| P | 접수확정시간 | 접수확정일시 계산 |
| Q | 약속일자 | 방문약속 참고 |
| R | 약속시간 | 방문약속일시 계산 |
| S | 이관일자 | 공장/센터 이관 참고 |
| T | 이관시간 | 이관일시 계산 |
| U | 센터수리일자 | 중수리/센터 완료 계산 |
| V | 센터수리시간 | 센터수리일시 계산 |
| W | 공장수리완료일자 | 서비스팩토리/공장 완료 계산 |
| X | 공장수리완료시간 | 공장수리완료일시 계산 |
| Y | 완료일자 | AS완료 당일/누적 계산 |
| Z | 완료시간 | 완료일시/리드타임 계산 |
| AA | 고장구분 | 고장 유형 보조 |
| AB | 고장코드(대) | 분석 확장용 |
| AC | 고장코드(중) | 분석 확장용 |
| AD | 고장코드(소) | 분석 확장용 |
| AE | 고장원인 | 분석 확장용 |
| AF | 실증상 | 분석 확장용 |
| AG | 처리내역 | 처리 내용 참고 |
| AH | 수리관련메모 | 메모 참고 |
| AI | 접수취소일자 | 접수취소 당일/누적 계산 |
| AJ | 접수취소시간 | 취소일시 계산 |
| AK | 접수취소사유 | 취소 사유 분석 |
| AL | 리드타임(고객) | 경수리 리드타임 평균 |
| AM | 리드타임(센터) | 중수리 센터 리드타임 |
| AN | 리드타임(이관) | 공장/이관 리드타임 |
| AO | 이관여부 | 이관 여부 판단 |
| AP | 지점코드 | 조직 분석 확장 |
| AQ | 지점 | 조직 분석 확장 |
| AR | 영업소코드 | 조직 분석 확장 |
| AS | 영업소 | 조직 분석 확장 |
| AT | 영업점코드 | 영업점 분석 확장 |
| AU | 영업점 | 영업점 분석 확장 |
| AV | 지구 | 지구 분석 확장 |
| AW | [고객/영업점장]명 | 연락/추적 참고 |
| AX | [고객/영업점]전화 | 연락/추적 참고 |
| AY | 마감대기/마감완료 | 마감 상태 참고 |

## 7. 앱 실적 계산에 필요한 핵심 컬럼
전체 원본은 51개 컬럼을 보관하되, 일일실적 계산에는 아래 컬럼이 핵심입니다.

| 원본열 | 컬럼명 | 계산 용도 |
| --- | --- | --- |
| A | 완료구분 | 완료/미완료 판단 보조 |
| B | 접수상태 | 접수대기/접수취소 판단 |
| C | 상태구분 | 미출동, 중수리, 센터/공장 상태 판단 |
| D | 접수번호 | 고유키 |
| E | 접수구분 | 일반A/S/이동 구분 |
| G | 접수분류 | 고장 유형 확장 |
| H | 파트 | 파트 필터 |
| I | 담당마스터코드 | 담당자 매핑 |
| J | 담당마스터 | 담당자 매핑 및 상세 행 |
| M | 접수일자 | 접수 당일/누적 |
| N | 접수시간 | 접수일시 |
| U | 센터수리일자 | 중수리 센터 완료/미완료 |
| V | 센터수리시간 | 센터수리일시 |
| W | 공장수리완료일자 | 서비스팩토리 완료 |
| X | 공장수리완료시간 | 공장수리완료일시 |
| Y | 완료일자 | 완료 당일/누적 |
| Z | 완료시간 | 완료일시 |
| AI | 접수취소일자 | 취소 당일/누적 |
| AJ | 접수취소시간 | 취소일시 |
| AL | 리드타임(고객) | 경수리 리드타임 |
| AM | 리드타임(센터) | 센터 중수리 리드타임 |
| AN | 리드타임(이관) | 공장/이관 리드타임 |
| AO | 이관여부 | 이관 여부 판단 |

## 8. 데이터 가공 테이블 설계
### 8.1 `AS_DAILY_WORK`
원본 raw에 앱 계산용 판단값을 추가한 작업 테이블입니다.

추천 가공 컬럼:
- 접수일
- 접수월
- 완료일
- 완료월
- 취소일
- 취소월
- 센터수리일
- 공장완료일
- 처리상태
- 완료유형
- 수리구분
- 파트그룹
- 센터명
- 미완료여부
- 접수대기여부
- 장기미완료여부
- 리드타임일수
- 센터리드타임일수
- 공장리드타임일수
- 집계대상여부

### 8.2 `AS_DAILY_SUMMARY`
앱 UI가 바로 읽는 최종 집계 테이블입니다.

추천 컬럼:
- 기준일
- 기준월
- 파트
- 수리구분
- rowType
- 표시명
- 센터명
- 담당마스터
- 접수당일
- 접수누적
- 완료미출동당일
- 완료미출동누적
- 완료출동당일
- 완료출동누적
- 완료당일
- 완료누적
- 미완료계
- 접수대기
- 미완료
- 미완료4일이상
- 리드타임평균
- 접수취소당일
- 접수취소누적
- 표시순서

## 9. 매핑 테이블 설계
### 9.1 `AS_MASTER_MAP`
기존 `일일실적`의 행 구조를 앱에서 재현하기 위한 매핑 테이블입니다.

추천 컬럼:
- 표시순서
- 수리구분
- 파트
- 센터명
- 담당마스터
- 담당마스터코드
- rowType
- 사용여부

### 9.2 현재 확인된 경수리 행 구조
- 전체(경수리)
- 서비스1
  - 하남센터: 김남길, 진태종, 강라성, 임정수, 조응호, 김수형, 임효석, 안기정
- 서비스2
  - 계양센터: 이기범, 이준혁, 마제승, 한인규, 이동우, 김홍기, 박준영
- 서비스3
  - 부산센터: 황득하, 이용섭, 박인수, 한혁빈
  - 대구센터: 김성휘, 강경민, 이신무
  - 경남센터: 김성진, 정순진
- 서비스4
  - 호남센터: 김광은, 김원균, 박영호
  - 충청센터: 송영준, 최상운, 김성용, 노승주
  - 제주센터: 서원혁

### 9.3 현재 확인된 중수리 행 구조
- 전체(중수리)
- 팩토리(본사)
- 서비스1(하남)
- 서비스2(계양)
- 서비스3(부산)
- 서비스4(호남)

## 10. 실적 계산 기준 초안
### 10.1 공통 기준
- 기준일은 사용자가 앱 화면에서 선택합니다.
- 누적 기준은 기준일이 속한 월의 1일부터 기준일까지입니다.
- 파트 필터는 `전체`, `서비스1`, `서비스2`, `서비스3`, `서비스4`, `서비스팩토리`를 제공합니다.
- 수리구분 필터는 `경수리`, `중수리`를 제공합니다.
- `서비스팩토리`는 `중수리`만 선택 가능합니다.

### 10.2 경수리 기준 초안
- 접수 당일: `접수일자 = 기준일`
- 접수 누적: `접수일자`가 월초~기준일 범위
- 완료 당일: `완료일자 = 기준일`
- 완료 누적: `완료일자`가 월초~기준일 범위
- 미출동: 완료 건 중 `상태구분 = 전화상담(미출동)`
- 출동: 완료 건 중 미출동이 아닌 건
- 미완료 계: 접수되었으나 완료/취소가 아닌 건
- 접수대기: 미완료 건 중 `접수상태 = 접수대기`
- 미완료: 미완료 계 - 접수대기
- 미완료 4일 이상: 기준일 - 접수일자 >= 4
- 접수취소 당일: `접수취소일자 = 기준일`
- 접수취소 누적: `접수취소일자`가 월초~기준일 범위
- 리드타임 평균: 완료 누적 건의 `리드타임(고객)` 평균

### 10.3 중수리 기준 초안
- 센터 중수리: `상태구분`이 `센터입고수리` 또는 `센터수리완료`인 건
- 팩토리 중수리: `상태구분`이 `공장입고수리` 또는 `공장수리완료(인수)`인 건
- 센터 완료 기준: `센터수리일자`
- 공장 완료 기준: `공장수리완료일자`
- 센터 리드타임: `리드타임(센터)` 또는 접수일시~센터수리일시 재계산
- 공장 리드타임: `리드타임(이관)` 또는 접수일시~공장수리완료일시 재계산

## 11. UI 설계 방향
### 11.1 최종적으로 선호한 방향
- 현대적인 모바일 앱 스타일
- 상단 그라데이션 헤더
- 필터는 pill 형태 또는 카드형 컨트롤
- 주요 KPI는 크게 표시
- 상세는 엑셀형 표보다 모바일 친화적인 리스트-테이블 하이브리드

### 11.2 화면 구성
- 상단 헤더
  - 뒤로가기
  - `AS 일일실적`
  - 기준일 표시
- 필터 영역
  - 날짜 선택
  - 파트 드롭다운
    - 전체
    - 서비스1
    - 서비스2
    - 서비스3
    - 서비스4
    - 서비스팩토리
  - 수리구분 드롭다운
    - 경수리
    - 중수리
  - 검색 버튼
  - 안내 문구: `서비스팩토리는 중수리만 선택 가능`
- KPI 영역
  - 접수
  - 완료
  - 미완료
  - 취소
  - LT
- 상세 영역
  - 상단에만 `구분 / 접수 / 완료 / 미완 / LT / 취소` 표시
  - 각 행은 숫자만 크게 표시
  - 전체/서비스/센터/담당자 행은 색상과 굵기로 구분

### 11.3 성능 관련 방향
- 색상 다양성 자체는 성능에 큰 영향을 주지 않습니다.
- 성능에 영향을 주는 주요 요소는 데이터 조회 횟수, Apps Script 집계 시간, DOM 렌더링 행 수입니다.
- 따라서 UI는 `AS_DAILY_SUMMARY`만 읽고, raw 전체를 매번 화면에서 계산하지 않는 방향이 좋습니다.
- 상세 행은 처음에는 요약 행 중심으로 표시하고, 필요시 센터/담당자 단위 확장도 고려합니다.

## 12. Apps Script 구현 방향 초안
### 12.1 서버 함수
- `getAsDailyStats(sessionToken, targetDate, part, repairType)`
  - `AS_DAILY_SUMMARY`를 읽어 UI용 JSON 반환
- `getAsFilterOptions(sessionToken)`
  - 파트/수리구분 옵션 반환
- `refreshAsDailyStats(sessionToken, targetDate)`
  - 추후 raw 업데이트 후 집계 재생성용으로 검토

### 12.2 프론트 함수
- `loadAsDailyStats()`
- `renderAsDailyKpis(data.summary)`
- `renderAsDailyRows(data.rows)`
- `handleAsPartChange()`
  - 파트가 `서비스팩토리`이면 수리구분을 `중수리`로 고정

### 12.3 예상 파일
- `AsDailyStats.html`
- `AsDailyStatsStyle.html`
- `JavaScript.html` 내 AS 실적 관련 함수 추가
- `Code.gs` 내 AS 실적 서버 함수 추가
- `index.html` include 추가

## 13. 앞으로 결정해야 할 항목
- [ ] raw 업로드/적재 방식 결정
  - 매일 xlsx를 Google Sheet로 붙여넣기
  - Apps Script로 xlsx 업로드 후 변환
  - 별도 Google Sheet 원본 시트에 자동 갱신
- [ ] `AS_MASTER_MAP` 관리 방식 결정
  - 코드 상수
  - Google Sheet 관리 테이블
- [ ] 중수리 판정 기준 최종 확정
- [ ] 서비스팩토리 집계 기준 최종 확정
- [ ] 리드타임 기준 최종 확정
  - raw의 리드타임 컬럼 사용
  - 앱에서 날짜/시간으로 재계산
- [ ] 미완료 4일 이상 기준 확정
  - 기준일 - 접수일자 >= 4
  - 영업일 기준 여부
- [ ] 앱 화면에서 담당자 행까지 항상 표시할지, 접기/펼치기로 표시할지 결정
- [ ] `AS_DAILY_SUMMARY`를 매번 계산할지, 일별 캐시/시트로 저장할지 결정

## 14. 현재 결론
- 무가공 raw의 `A~AY` 51개 컬럼은 `AS_RAW_UNIFIED`에 그대로 보관하는 것이 좋습니다.
- 기존 엑셀의 `접수RAW`와 `RAW`는 하나로 통합 가능합니다.
- 앱에서는 raw를 직접 복잡하게 계산하지 않고, `AS_DAILY_WORK`와 `AS_DAILY_SUMMARY`를 거쳐 화면에 표시하는 구조가 안정적입니다.
- UI는 현대적인 모바일 대시보드 스타일로 진행하되, 상세 숫자는 크게 표시하고 컬럼 라벨은 상단에만 표시하는 방향이 좋습니다.

## 15. 확정 운영 기준
### 15.1 raw 데이터 붙여넣기 방식
- `AS_RAW_UNIFIED` 시트에는 매일 시스템에서 받은 무가공 raw 데이터를 그대로 복사/붙여넣기 합니다.
- 복사/붙여넣기 시 raw 헤더를 포함합니다.
- `AS_RAW_UNIFIED`의 1행은 항상 원본 헤더입니다.
- `AS_RAW_UNIFIED`의 2행부터 실제 데이터입니다.
- 원본 컬럼 범위는 `A:AY` 51개 컬럼입니다.
- 컬럼 순서는 시스템 raw 원본 순서를 그대로 유지합니다.
- `AS_RAW_UNIFIED`에는 수식이나 앱 계산용 보조 컬럼을 추가하지 않습니다.
- 앱 계산용 수식은 `AS_DAILY_WORK`에서 `AS_RAW_UNIFIED!A2:AY`를 참조해 작성합니다.

### 15.2 확정된 참조 기준
```text
AS_RAW_UNIFIED!A1:AY1 = 원본 헤더
AS_RAW_UNIFIED!A2:AY  = 원본 데이터
```

### 15.3 수식 작성 기준
- `AS_DAILY_WORK`는 `AS_RAW_UNIFIED`의 2행부터 데이터를 참조합니다.
- `접수번호`가 비어 있으면 빈 행으로 처리합니다.
- 기본 수식 패턴은 아래 형태를 사용합니다.

```gs
=ARRAYFORMULA(IF(AS_RAW_UNIFIED!D2:D="",,AS_RAW_UNIFIED!D2:D))
```

### 15.4 운영상 주의사항
- 매일 raw를 붙여넣을 때 기존 데이터는 삭제 후 새 데이터로 교체합니다.
- 헤더가 포함되어야 하므로 데이터는 항상 `A1`부터 붙여넣습니다.
- 시스템 raw의 컬럼 순서가 바뀌면 `AS_DAILY_WORK` 수식과 앱 집계가 깨질 수 있으므로, 헤더 검증 로직을 추후 Apps Script에 추가하는 것을 권장합니다.
- 원본 보존과 계산 안정성을 위해 `AS_RAW_UNIFIED`에는 절대 수식 컬럼을 섞지 않습니다.
