# 🔍 검색 및 필터 기능 구현 체크리스트 (Phase 4)

> **목표**: 정적인 UI에 실제(Mock) 데이터를 연동하고, 검색 및 필터링 로직을 구현합니다.
> **예상 소요 시간**: 2~3일

---

## 1. 🔍 [검색] BS 및 리워크 로직 구현
- [ ] **데이터 구조 정의** (`types/index.ts`)
  - `BsItem` 인터페이스 정의 (서비스 유형, 날짜, 고객명, 증상 등)
- [ ] **Mock 데이터 생성** (`services/mockData.ts`)
  - 다양한 케이스의 더미 데이터 20~30개 추가
- [ ] **필터링 로직 구현** (`app/bs-search/index.tsx`)
  - 선택된 4개 서비스 옵션(`service1`~`service4`) 기반 필터링 함수 구현
  - "검색" 버튼 클릭 시 필터링 실행
  - 결과 없을 때 "검색 결과가 없습니다" 표시
- [ ] **상세 보기 모달/화면** (선택 사항)
  - 리스트 아이템 클릭 시 상세 정보 표시 (Alert 또는 Modal)

---

## 2. 📍 영업점 주소 검색 로직
- [ ] **검색 로직 구현** (`app/store-directory.tsx`)
  - 검색어 입력 시 실시간/엔터 키로 필터링
  - 영업점명, 주소 키워드 검색 지원
- [ ] **전화 걸기 기능** (`utils/phoneCall.ts`)
  - `Linking.openURL('tel:...')` 활용하여 전화 걸기 연동

---

## 3. 🚛 배차 목록 날짜 필터링
- [ ] **날짜 데이터 연동** (`app/dispatch.tsx`)
  - 선택한 날짜(`YYYY-MM-DD`)와 일치하는 배차 데이터만 표시
- [ ] **상태별 정렬** (옵션)
  - 대기중 > 진행중 > 완료 순으로 정렬 로직 추가

---

## 4. 📊 실적 및 통계 대시보드 (Mock)
- [ ] **AS 실적 데이터 연동** (`app/statistics/as-performance.tsx`)
  - 이번 달 배정/처리/미처리 건수 계산 로직
- [ ] **CS 통계 데이터 연동** (`app/statistics/cs-stats.tsx`)
  - 주요 불량 유형 Top 5 계산 로직

---

## 5. 🧪 통합 테스트
- [ ] **시나리오 테스트**
  - BS 검색: 서비스1 선택 -> 검색 -> 결과 확인
  - 영업점 검색: "서울" 입력 -> 서울 지역 영업점만 표시 확인
  - 배차 목록: 특정 날짜 선택 -> 해당 날짜 데이터 표시 확인
